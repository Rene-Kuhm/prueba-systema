const config = {
    firebase: {
        projectId: 'cospecreclamos', // Replace with your actual Firebase project ID
        functionUrl: 'https://us-central1-your-firebase-project-id.cloudfunctions.net/sendClaimNotification'
    }
};

export default config;
